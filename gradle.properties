org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# All three artifacts share one version (lockstep). The release version is supplied at
# publish time via -PVERSION_NAME (see docs/BUILDING_AND_RELEASING.md).
